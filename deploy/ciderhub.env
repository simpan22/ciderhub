DATABASE_URL=sqlite:///var/lib/ciderhub/ciderhub.db
BIND_ADDR=127.0.0.1:8091
# Placeholder only — this file is tracked in git. The real value lives
# solely in /etc/ciderhub/ciderhub.env on the server (edited directly
# over SSH), never committed. deploy.sh doesn't touch that file on
# ongoing deploys, only the one-time setup in deploy/README.md does.
APP_PASSWORD=changeme
